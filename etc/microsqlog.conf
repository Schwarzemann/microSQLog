# Optional. If empty, the daemon discovers systemd services containing
# "mysql" or "mariadb" in their unit names.
managed_units =

check_interval_seconds = 30
restart_grace_seconds = 8
journal_lines = 80
log_file = /etc/microsqlog/critical.log
